[project]
name = "moovent-stack"
version = "0.4.22"
description = "Moovent internal developer stack launcher"
readme = "README.md"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]

