[project]
name = "moovent-stack"
version = "0.5.0b1"
description = "Moovent internal developer stack launcher"
readme = "README.md"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]

